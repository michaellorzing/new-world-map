/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['placeimg.com', 'images.unsplash.com', 'unsplash.com'],
	},
};

module.exports = nextConfig;
